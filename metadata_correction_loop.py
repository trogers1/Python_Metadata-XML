#maybe use terminal to iterate the .py program for each file.
#use the etree function from the lxml module to find the right tag in the metadata file
#then use if, elif, else thing.

if len(filepart) < 1:
    .write ("Whatever it is that I need to write") 
